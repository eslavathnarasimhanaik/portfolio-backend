# Active profile selection (Choose either mysql OR h2)
# Set to 'h2' to run the backend instantly without installing MySQL!
# On Render/production: set SPRING_PROFILES_ACTIVE=mysql as env variable
spring.profiles.active=${SPRING_PROFILES_ACTIVE:mysql}
